In the notebook blackbody_stellar_colors.ipny (both in the notebook and notebook_evaluated

in the code cell starting with:
# ── 4. Extract selected MILES spectra and plot the spectral sequence ───────
# Wavelength grid: CRVAL1=3500 Å, CDELT1=0.9 Å, NAXIS1=4367 pixels
# Coverage: 3500–7430 Å  (350–743 nm)

Add labels on every panel about the absorption lines and bands using pyNEB
https://pypi.org/project/pyneb/
https://morisset.github.io/PyNeb_devel/
Follow this example to plot the line list:
https://github.com/JohanComparat/qmost_templates/blob/main/python/plot_full.py
but extend to lines relevant for stars .
Already some lines are present, more are welcome.





In the notebook blackbody_stellar_colors.ipny (both in the notebook and notebook_evaluated

Section 8. Stellar Population Synthesis, add a new Section


Add the Chabrier 2003 initial mass function and use it. https://arxiv.org/abs/astro-ph/0304382
update the figures

About the statement:
"The main-sequence lifetime scales approximately...", read : 
https://ui.adsabs.harvard.edu/abs/1989A%26A...210..155M/abstract
Add a summary table with order of magnitude as a function of mass

Add a cell to make a prediction, not with the black body, but with the MILES spectra.








In the notebook blackbody_stellar_colors.ipny (both in the notebook and notebook_evaluated

After section 8. Stellar Population Synthesis, add a new Section

Section 9. Observed galaxy spectra.

use the different templates made by :
https://github.com/JohanComparat/qmost_templates
to show the star-forming galaxy and quiescent galaxy templates in the rest-frame (redshift=0).
with lines. 

Fit two SSP models: one with black body and one with MILES. 
to show the model and the data.
It is a simple fit 

Then do a fit using ppxf
* https://pypi.org/project/ppxf/ https://ui.adsabs.harvard.edu/abs/2004PASP..116..138C/abstract




Merge the notebooks:
galaxy_spectra_redshift.ipynb
photometric_redshifts.ipynb

into a single notebook:
redshift_measurement.ipynb


For both tasks below, use the four templates star-forming galaxy, quiescent galaxy, AGN type1 and AGN type 2 :
https://github.com/JohanComparat/qmost_templates
they are in the rest-frame. 
Shift them to a few redshifts, e.g. z=[0.1, 0.3, 0.5]
For each create an observed spectrum, rescale to an observed r-band magnitude of r=20, add a couple levels of noise [1%, 10%, 50%] in the relative flux in the r-band. 
predict the corresponding three observed broad bands: g,r,i and uncertainties.

The goal of this notebook is to explain pedagogically :
1. how a spectroscopic redshift is measured on DESI spectra with the DESI software
https://github.com/desihub/redrock
https://github.com/desihub/tutorials
https://data.desi.lbl.gov/doc/

2. how a photometric redshift is measured on with EAZY-py
https://github.com/gbrammer/eazy-py
https://eazy-py.readthedocs.io/en/latest/

show the success and limitation of each method.



In the notebook emission_stars_galaxies.ipynb (both in the notebook and notebook_evaluated)

In the first text cells,
'Remove the DATA: ...' statement
Update the References, and put them after the theoretical background of the notebook
Update the learning objectives, the notebook title to reflect the full content of the notebook
Similarly update the theoretical background
